// import { isType } from 'type-plus'
// import { Plugin } from './plugin.types.js'

// describe('Plugin.MergeResultContext', () => {
// 	it('returns PluginContext of a single Plugin', () => {
// 		// type R = Plugin.MergeResultContext<[Plugin<[], [], [], { a: number }>]>

// 		// isType.equal<true, { a: number }, R>()
// 	})

// 	it('merges PluginContext of 2 plugins', () => {
// 		// type R = Plugin.MergeResultContext<[Plugin<[], [], [], { a: number }>, Plugin<[], [], [], { b: number }>]>
// 		// isType.equal<true, { a: number; b: number }, R>()
// 	})
// })

it.todo('no test')
