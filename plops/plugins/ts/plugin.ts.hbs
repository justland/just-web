import type { LogContext } from '@just-web/log'
import { definePlugin, PluginContext } from '@just-web/types'

export type {{pascalCase name}}Options = {
  {{name}}?: 1
}

const {{camelCase name}}Plugin = definePlugin((options?: {{pascalCase name}}Options) => ({
  name: '@just-web/{{name}}',
  init(ctx: LogContext) {
    return [{ {{name}}: 1 }, ctx]
  },
  start(ctx) { }
}))

export type {{pascalCase name}}Context =  PluginContext<typeof {{camelCase name}}Plugin>

export default {{camelCase name}}Plugin
